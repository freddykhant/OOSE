package edu.curtin.transport.model;

/**
 * Observer Interface that defines the update() method, which is used for notifying observers about changes in the vehicle.
 */

public interface TransportObserver
{
    void update();
}